{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend nodejs for all app",
  "main": "server.js",
  "scripts": {
    "start": "nodemon src/server.js  exec babel-node --presets env" ,
    "test": "test",
    "build":"babel src -d dist source-maps",
    "server": "node dist/server.js" 
  },
  "author": "masioudi gbadamassi",
  "license": "ISC",
  "devDependencies": {
    "@babel/preset-react": "^7.8.3",
    "nodemon": "^2.0.2"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
